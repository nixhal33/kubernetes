apiVersion: v1
kind: Service
metadata:
  name: bank-app
spec:
  type: ClusterIP
  selector:
    app: bank-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30083

