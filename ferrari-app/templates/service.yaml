apiVersion: v1
kind: Service
metadata:
  name: {{ include "ferrari-app.fullname" . }}
  labels:
    {{- include "ferrari-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ferrari-app.selectorLabels" . | nindent 4 }}
