apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecommerce-site.fullname" . }}
  labels:
    {{- include "ecommerce-site.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ecommerce-site.selectorLabels" . | nindent 4 }}
